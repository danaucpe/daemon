name = "daniel-black-daemon"
main = "src/worker.ts"
compatibility_date = "2025-08-20"
workers_dev = true

[observability]
enabled = false

[observability.logs]
enabled = false

# Using workers.dev subdomain for now
# Add custom domain later: routes = [{ pattern = "daemon.danielblack.com", custom_domain = true }]

[site]
bucket = "./dist"
